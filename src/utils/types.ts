export interface UserInfo {
    uid: string,
    username: string,
    nickname: string,
    phoneNumber: string,
    email: string,
    avatarUrl: string,
    refreshToken: string
    accessToken: string
}